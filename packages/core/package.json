{
  "name": "@aegis/core",
  "version": "0.1.0",
  "description": "AEGIS runtime engines: ledger, registries, assessment, decision, enforcement, runtime safety, recovery, learning, research",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test 'dist/test/**/*.test.js'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aegis/contracts": "*",
    "@supabase/supabase-js": "^2.110.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.6.0"
  }
}
